import { MenuInitialState } from "../types/menu";

export const initialState: MenuInitialState = {
  loading: false,
  upcomingMeals: [],
  servedOrders: [],
  sevenDaysMenu: [],
};

export const previousState = {};